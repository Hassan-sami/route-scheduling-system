{
  "name": "route-scheduling-system",
  "version": "1.0.0",
  "description": "A REST API for Route Scheduling System with Prisma ORM",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "db:push": "npx prisma db push",
    "db:seed": "node prisma/seed.js",
    "db:studio": "npx prisma studio",
    "db:migrate": "npx prisma migrate dev --name init"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@prisma/client": "^5.5.2",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "helmet": "^7.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "prisma": "^5.5.2"
  }
}